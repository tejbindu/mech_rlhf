run:
  output_dir: /scratch4/workspace/ypathak_umass_edu-pref-steer/mech_pref_artifacts

model:
  dtype: fp16

data:
  direction_model: beaver
  harm_category: all
  min_severity: 2
  safe_max_severity: 0

activations:
  pooling: response_first
  batch_size: 1             # V100 32GB memory constraint
